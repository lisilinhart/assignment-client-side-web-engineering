// import { fromJS } from 'immutable';
// import { makeSelectFormulaOneScreenDomain } from '../selectors';

// const selector = makeSelectFormulaOneScreenDomain();

describe('makeSelectFormulaOneScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
