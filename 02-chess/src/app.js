// import io  from 'socket.io-client';
// import config from './config'
import './chess'

// // create new socket for the server
// const socket = io(config.SERVER_URL);

// // add listeners to socket to interact with the server
// socket.on('game created', (data) => {
//   console.log('game created', data);
// });

// socket.on('game joined', function(data) {
//   console.log('game joined', data);
//   initGame(data.game, socket);
//   // board.position(data.game.fen);
// });

// socket.on('move', function(data) {
//   console.log('move', data);
//   // console.log('data.move', data.move);
//   update(data.move, socket);
//   // chess.move(data.move);
//   // board.position(chess.fen());
// });


// socket.emit('join game', { game: "1bcrpd3p" });

// // updateStatus();
